<% if current_user.following?(@user) %>
  <%= button_to 'Unfollow', unfollow_path(profile_id), method: :delete %>
<% else %>
  <%= button_to 'Follow', follow_path(profile_id), method: :post %>
<% end %>